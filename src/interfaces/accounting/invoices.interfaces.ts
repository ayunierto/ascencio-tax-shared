export enum BillToType {
  COMPANY = 'company',
  EMPLOYEE = 'employee',
  CUSTOM = 'custom',
}

export enum InvoiceStatus {
  PENDING = 'pending',
  PAID = 'paid',
  OVERDUE = 'overdue',
  CANCELED = 'canceled',
}
